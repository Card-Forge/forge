package forge.game.decision;

/** The supported atomic TARGET alternatives in the controlled ForgeRL slice. */
public enum TargetCandidateKind {
    TARGET_CARD,
    TARGET_PLAYER,
    TARGET_STACK_OBJECT,
    DONE
}
