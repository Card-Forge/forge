package forge.game.decision;

/** The top-level action selected while a player has priority. */
public enum PriorityActionKind {
    PASS,
    PLAY_LAND,
    CAST_SPELL,
    ACTIVATE_ABILITY,
    ACTIVATE_MANA_ABILITY,
    SPECIAL_ACTION
}
