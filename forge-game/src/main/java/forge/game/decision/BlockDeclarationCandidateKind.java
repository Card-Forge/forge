package forge.game.decision;

/** Atomic candidate operation in a neutral BLOCK declaration session. */
public enum BlockDeclarationCandidateKind {
    CHOOSE_BLOCKER,
    CHOOSE_ATTACKER_FOR_BLOCKER,
    DONE
}
