package forge.game.decision;

/** The observed controller-callback family reached while announcing a selected priority action. */
public enum DownstreamCallbackFamily {
    MODE,
    X_VALUE,
    TARGET,
    PAYMENT,
    CONFIRMATION,
    CARD_SELECTION,
    OTHER
}
