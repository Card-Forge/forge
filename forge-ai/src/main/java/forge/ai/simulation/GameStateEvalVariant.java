package forge.ai.simulation;

/**
 * Selectable weight bundle for {@link GameStateEvaluator}. Process-wide; configure before matches for comparable batches.
 */
public enum GameStateEvalVariant {
    A,
    B,
}
