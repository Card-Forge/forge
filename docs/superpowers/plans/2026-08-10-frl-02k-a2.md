# FRL-02K-A2 Confirmation Authority and Optional-Trigger Closure Audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correct the ML authority document and produce reproducible, audit-only evidence that either closes or rejects `OPTIONAL_TRIGGER_NO_COST` as the first FRL-02K adapter candidate.

**Architecture:** Rebase only `frl/02k-confirmation-boundary` onto the docs-only `origin/master` commit. Keep all production decision classes and callback implementations unchanged. Use test-only fixtures and narrow audit instrumentation to classify the existing 26 reactive callbacks, prove public context/identity/continuation constraints, then update the authority and audit documents with an explicit verdict.

**Tech Stack:** Git/rebase, Java 17, Maven/Surefire/TestNG, Forge trigger engine, existing `DECISION_TRACE_V2`, `PriorityActionDiagnostics`, and deterministic fresh-JVM simulation workloads.

---

### Task 1: Verify and preserve repository boundaries

**Files:** Read only in `C:\forgeAI-confirmation`, `C:\forgeAI`, and `C:\forgeAI-determinism-gate`.

- [x] Run `git status`, branch, HEAD, `git fetch origin`, `git rev-parse origin/master`, and `git log --oneline --decorate -5` in `C:\forgeAI-confirmation`; require branch `frl/02k-confirmation-boundary`, HEAD `b4d6f4abf578d8727b681c09ed83b1edcf2cab92`, and `origin/master` `266f44a7cae8f9cc7379a8429a137c5fc7c483bb` before rebase.
- [x] Read `git -C C:\forgeAI status --short --branch` and `git -C C:\forgeAI-determinism-gate status --short --branch`; do not reset, clean, stash, switch, delete, or write in either protected worktree.
- [x] Run `git rebase 266f44a7cae8f9cc7379a8429a137c5fc7c483bb`; require `git merge-base HEAD origin/master` to equal that SHA and record rewritten audit commit SHAs.

### Task 2: Repair authority and evidence labels

**Files:** Modify `docs/AI-ML DOCS/ML_STRATEGY.md` and `docs/AI-ML DOCS/FRL_02K_CONFIRMATION_AUDIT.md` only.

- [x] Inspect current statements with `rg -n -C 4 "Revision|CONFIRMATION|ORDER|DAMAGE_ASSIGNMENT|RandomLegalPolicy|PAYMENT|286|287|teacher" "docs/AI-ML DOCS/ML_STRATEGY.md"` and `rg -n -C 3 "287|286|CONFIRMATION complete|NO_SAFE_V0_YET|baseline" "docs/AI-ML DOCS/FRL_02K_CONFIRMATION_AUDIT.md"`.
- [x] Update ML strategy to Revision 10: FRL-02K0 PASS, FRL-02K attribution audit PASS, production implementation OPEN; replace generic `CONFIRMATION -> Enumerate` with a semantic adapter-qualified statement; place semantic closure and CONFIRMATION implementation before ORDER; preserve valid ORDER, DAMAGE_ASSIGNMENT, zero-unsupported, PAYMENT, DECISION_TRACE_V2, and teacher-label language.
- [x] Correct audit labels to `K0/base = 286`, `pre-A2 audit branch = 287`, and never call 287 the original master baseline.
- [x] Run `git diff --check` and inspect both documentation diffs before adding further evidence.

### Task 3: Reconcile every reactive trigger callback

**Files:** Use ignored output under `target/frl02k-a2-audit`; add only test files under `forge-gui-desktop/src/test/java/forge/ai/ability/` if required. Do not modify production callback, trigger, decision, or diagnostics classes.

- [x] Re-run Izzet Guild Kit vs Dimir Guild Kit, seed `20260810`, 10 games, and Dead and Alive vs Air Forces, seed `20260809`, 10 games, in fresh JVMs with narrow method attribution.
- [x] At the engine-owned `WrappedAbility.resolve` audit seam, capture safe categories for game index, turn, phase, player seats, source-zone category, optional/mandatory classification, decider, static/delayed/generated/copied provenance, wrapped API, triggering `AbilityKey` names/types, continuation presence, and native boolean result. Never emit hidden identities.
- [x] Assign all 26 reactive occurrences to mutually exclusive buckets: `NORMAL_OPTIONAL_NO_COST_PUBLIC`, `NORMAL_OPTIONAL_NO_COST_CONTEXT_UNSUPPORTED`, `COST_BEARING_OPTIONAL`, `STATIC_OPTIONAL`, `DELAYED_OPTIONAL`, `GENERATED_OR_COPIED_OPTIONAL`, `HIDDEN_SOURCE`, `HIDDEN_TRIGGERING_OBJECT`, or `OTHER`; assert the sum is exactly 26.
- [x] Instrument the engine seam rather than `PlayerControllerAi.confirmTrigger` and prove `chooseContraptionsToCrank` helper calls cannot satisfy the normal-trigger admission path.

### Task 4: Close identity, occurrence, context, and visibility gates

**Files:** Read `Trigger.java`, `WrappedAbility.java`, `SpellAbility.java`, `Card.java`, and `CardView.java`; add only test-only fixtures under `forge-gui-desktop/src/test/java/forge/ai/ability/`.

- [x] Prove whether a stable trigger-definition ordinal survives fresh JVMs, copying, and admitted card-state transitions; otherwise use a normalized semantic-definition hash plus stable visible source/card-state identity. Reject `Trigger.getId`, `hashCode`, `toString`, localized descriptions, and Java object identity.
- [x] Define a deterministic trace-local monotonic `triggerSessionIndex`/`occurrenceIndex` for simultaneous instances. It must not be a semantic candidate key and must not depend on PID, time, RNG, or `Trigger.getId`; prove same-seed fresh-JVM equality.
- [x] For every public candidate, list each observed `AbilityKey`, runtime semantic type, decision relevance, decider visibility, and approved neutral encoding. Encode only explicit public identities such as source card, player, attacker, defender, or public triggering card; never expose raw runtime objects or serialize the whole map.
- [x] Add anti-aliasing evidence where the same source/definition with different public triggering objects yields distinct conceptual context, plus rejection fixtures for opponent hidden-zone source, face-down source, and hidden triggering card without emitting hidden identity.

### Task 5: Prove lifecycle, exactly-once, and neutrality constraints

**Files:** Read `ActionContinuation.java` and existing diagnostics tests; add only test-only scaffolding.

- [x] Assert the actual continuation equivalent is absent at every supported ordinary optional trigger and report the exact non-null count; any unexplained nonzero count forces `NO_SAFE_V0_YET`.
- [x] Enforce the future admission predicate: decider present, wrapper optional, normal nonstatic lifecycle, supported intrinsic provenance, no nonzero cost, public source, stable definition and occurrence identity, public decision-relevant objects, and no active continuation. Reuse Forge’s classification instead of adding a second cost parser.
- [x] Prove rejection of mandatory, nonzero-cost, optional-static, delayed/player-defined-delayed, generated/copied/granted, hidden, and helper-originated paths. Preserve the existing `OptionalDecider`-before-`Cost==0` branch ordering.
- [x] With test scaffolding, prove capture-before-callback, one native callback, result-after-callback, no second `doTrigger`, no replay, no target mutation, no RNG draw, unchanged `FORGE_STATE_V1`, unchanged RNG count, and exception isolation.

### Task 6: Update reports and choose one architecture verdict

**Files:** Modify `docs/AI-ML DOCS/FRL_02K_CONFIRMATION_AUDIT.md`; modify ML strategy only for authority correction.

- [x] Add an A2 section without erasing A1 history: authority correction, 286/287 labels, exact 26-bucket table, identity/occurrence decisions, context matrix, visibility evidence, continuation count, admission predicate, exclusions, exactly-once proof, neutrality, blocker ledger, and `DECISION_TRACE_V2` compatibility.
- [x] Keep optional no-cost trigger, cost-bearing trigger, `confirmAction`, `confirmPayment`, `chooseBinary`, prevention payment, bid, replacement, and static application separate in the blocker ledger.
- [x] Select exactly one verdict: `IMPLEMENT_OPTIONAL_TRIGGER_NO_COST_V0` only if every closure gate passes; otherwise `NO_SAFE_V0_YET` with the exact remaining blocker. Do not implement any production adapter.

### Task 7: Verify and preserve audit-only Git history

**Files:** Only authority/report/test/plan files created by this milestone may be committed.

- [x] Run focused A2 tests, the 286-test K0/base selection, the 287-test pre-A2 selection, any new expanded selection, `FullGameCollectorNeutralityTest`, `WorkerIsolationSmokeTest`, package, configured Checkstyle lifecycle, and `git diff --check`; record exact passed/failed/error/skipped counts.
- [x] Confirm no production implementation file changed and both protected worktrees remain as initially observed.
- [ ] Commit authority, test, and report changes intentionally; verify a clean audit worktree.
- [ ] If all work is clean and audit-only, push `frl/02k-confirmation-boundary` and create/update only a Draft PR. Do not mark ready, merge, implement `DecisionType.CONFIRMATION`, or begin ORDER/DAMAGE_ASSIGNMENT.
