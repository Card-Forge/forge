# Network Debug Configuration
# This file controls network debugging, bandwidth logging, and performance monitoring.
#
# Changes to this file require restarting Forge to take effect.

# ============================================================
# Bandwidth Logging
# ============================================================
# Enable bandwidth tracking and delta sync comparison logging.
# When enabled, shows three measurements per packet:
#   - Approximate size (delta algorithm efficiency)
#   - Actual network bytes (ground truth with compression)
#   - Full state estimate (baseline for comparison)
#
# Console output example:
#   [DeltaSync] Packet #1: Approximate=320 bytes, ActualNetwork=450 bytes, FullState=1200 bytes
#   [DeltaSync]   Savings: Approximate=73%, Actual=62% | Cumulative: ...
#
# Performance impact: ~1-2% when enabled, 0% when disabled
# Default: true (enabled for development/debugging)
bandwidth.logging.enabled=true

# ============================================================
# Network Debug Logger
# ============================================================
# Enable/disable debug logging for network synchronization.
# When disabled, no log files are created and no console output is shown.
# Default: true (enabled for development/debugging)
debug.logger.enabled=true

# Console log level: DEBUG, INFO, WARN, ERROR
# Controls what is printed to the console (System.out)
# DEBUG = Very verbose (hex dumps, property details, collections)
# INFO  = Normal operation (sync start/end, summaries)
# WARN  = Potential issues (missing objects, unexpected states)
# ERROR = Failures and exceptions
# Default: INFO (shows important events without flooding console)
debug.logger.console.level=INFO

# File log level: DEBUG, INFO, WARN, ERROR
# Controls what is written to the log file (logs/network-debug-*.log)
# Default: DEBUG (captures everything for post-mortem analysis)
debug.logger.file.level=DEBUG

# ============================================================
# Notes for Production Deployment
# ============================================================
# For production releases, consider:
#   - bandwidth.logging.enabled=false (reduces overhead)
#   - debug.logger.enabled=false (no log files)
#   - debug.logger.console.level=ERROR (only show critical issues)
#
# For development/testing, current defaults are recommended.
