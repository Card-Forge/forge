# Relocate the ThreeTen backport into the java.time namespace so upstream
# java.time references resolve against real app-supplied classes on MobiVM
# (which has no java.time at all). Resource TZDB.dat is duplicated to both
# paths by the pipeline script.
type org/threeten/bp/ java/time/
